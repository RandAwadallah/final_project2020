<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mobile Phones"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".update_mobile"
            android:parentActivityName=".update_list"></activity>
        <activity android:name=".update_list"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".details"
            android:parentActivityName=".listofmobilesActivity" />
        <activity
            android:name=".listofmobilesActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".InsertActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>